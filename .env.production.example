# Authentication (Clerk)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
CLERK_SECRET_KEY=your_clerk_secret_key

# AWS SES Configuration
AWS_ACCESS_KEY=your_aws_access_key
AWS_SECRET_KEY=your_aws_secret_key

# Stripe Configuration
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# Application Configuration
NEXT_PUBLIC_WEBSITE_URL=your_website_url  # Will be set automatically by Render using ${RENDER_EXTERNAL_URL}
JWT_SECRET_KEY=your_jwt_secret_key  # Generate a secure random key for production
MONGODB_URI=your_mongodb_uri  # Use your production database URI
ZEROBOUNCE_API_KEY=your_zerobounce_api_key

# Production Setup Instructions:
# 1. Never commit this file with real credentials
# 2. Copy this file to .env.local for local development
# 3. For production deployment:
#    - Use Render's environment variables dashboard to securely set all credentials
#    - Do not store sensitive values in render.yaml
#    - Ensure all keys are production credentials, not test ones
#
# Security Checklist:
# ✓ Use production Clerk keys from your Clerk dashboard
# ✓ Configure AWS SES for production (ensure out of sandbox mode)
# ✓ Use production Stripe keys
# ✓ Update Stripe webhook endpoints for production
# ✓ Use a separate production MongoDB database
# ✓ Generate a strong random JWT secret
# ✓ Enable IP whitelisting for MongoDB if needed
# ✓ Set up monitoring and logging
